language: nb
intents:
  HassTurnOn:
    data:
      - sentences:
          - "Kjør <navn> [skript]"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
